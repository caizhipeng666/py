# 列表

### 迭代列表
func|desc
---|---
chain|拼接
accumulate|累加
compress|漏斗


### chain
```
list(chain(['I','love'],['python'],['very', 'much']))
['I', 'love', 'python', 'very', 'much']
```

### accumulate
```
accumulate(iterable, func)
```
